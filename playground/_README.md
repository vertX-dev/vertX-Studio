# Playground

Drop a folder here with an `index.html` in it and it goes live at `/playground/<folder>/`.
See the site README for `meta.json`.

Do not put files directly in this folder — only subfolders. `/playground` itself is the
generated index page.
